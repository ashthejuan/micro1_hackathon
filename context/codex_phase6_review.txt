You are reviewing the Phase 6 implementation of the "Agentic Incident Postmortem Synthesizer" (a hackathon agentic-workflow entry). CODE REVIEW — do NOT modify files. Read them, critique, and report bugs, correctness issues, design risks, and test-coverage gaps with file:line evidence.

Files to review (project root):
- graph.py  (LangGraph StateGraph binding the 7 async agents.py nodes; arun_incident + CLI entrypoint)
- tests/test_graph_integration.py  (Phase 6 integration tests; the full suite is ~69 passing, Phase 6 adds 4 tests — assume green unless you find a real failure)

Verified real API contracts these depend on (do NOT assume otherwise):
- agents.py nodes: each `async def node(state: AgentState, *, llm, tracer, conn, collection) -> dict`. ingest_node writes SQLite + fires recall_incidents via asyncio.create_task (returns {"recall_task": Task}); timeline_node returns {"timeline_events"}; analyze_node awaits state["recall_task"] and returns {"candidates", "consulted"}; writer_node returns {"postmortem"} (citation guard retries once); verifier_node calls store.verify_postmortem(conn, incident_id, pm, recalled_incident_ids=...) and returns {"verification"}; human_gate_node returns {"human_decision", "applied_incidents", "consulted"} (rejects if verification_score < 1.0 OR consult-only leak); memory_writer_node embeds non-leaking postmortems via memory.store_memory (returns {}).
- AgentState is a TypedDict(total=False) with: incident_id, window_start, window_end, description, evidence: List[Evidence], recall_task: asyncio.Task, timeline_events, candidates, consulted, postmortem, verification, human_decision, applied_incidents. recall_task is the lone non-data field (asyncio.Task); LangGraph 1.2.11 passes it through state untouched during the parallel Send fan-out (this was empirically verified — no state-copy crash).
- memory.recall_incidents(symptom_text, collection, n=5) -> List[ConsultedIncident]; memory.store_memory(postmortem, collection, verification_score=None).
- store.connect(":memory:") -> sqlite3.Connection (row factory, foreign_keys ON); store.upsert_postmortem(...) persists the postmortem row with status/approved_by/time_approved.
- llm_adapter.LLMAdapter(live=...). chat(system, messages, model, params, schema) is async -> schema instance. embed(texts) async -> vectors.
- rag.create_memory_collection(name, embed_fn, backend="auto"|"memory"|"chroma") -> object with async add(id,document,metadata), async recall(text,n,where), sync count().
- schemas: Evidence(id, incident_id, ts, source, source_url, content); Postmortem(incident_id, summary, impact, root_cause, timeline, action_items, claims, consulted_incidents); VerificationReport(incident_id, claim_reports, verification_score); ConsultedIncident(incident_id, similarity_score, applied, note).
- helpers.py: FakeLLMAdapter (scripted TimelineEvent/RootCauseCandidate/Postmortem keyed by schema; embed -> hash vectors), INCS dict of incidents, make_collection() (in-memory backend), seed_prior(collection) (seeds INC-002).

Spec context (hackathon §B.4 + PRD):
- §B.4 requires representative trajectories per agent (instructions->result, tool responses, retries, human checkpoints) — satisfied by tracer.py writing trajectories/{incident_id}/{agent}.json + manifest.json. The graph must produce all 7 agent trajectory files per run.
- Async fan-out requirement: timeline + analyze run as a PARALLEL Send fan-out after ingest, merged before writer. This must actually execute both branches (not serial).
- Reproducibility (15 pts): eval must run offline with no key. The fake-adapter E2E must always run; the real-LLM replay test must SKIP cleanly when fixtures/llm_cache.jsonl is absent.

Verify and report specifically:
1. Graph topology correctness: START->ingest, ingest->[Send timeline, Send analyze], [timeline,analyze]->writer, writer->verifier, verifier->human_gate, human_gate->memory_writer, memory_writer->END. Is the fan-out actually parallel (Send used, not sequential add_edge)? Does the fan-in add_edge([...], "writer") rely on a LangGraph version that supports list fan-in (1.2.11 does — confirmed)? Any risk the writer runs before both branches finish?
2. _bind closure (graph.py:42-46): it wraps each node to swallow (state, config) and inject config. Is config unused/ignored correctly? Any issue with LangGraph passing a second positional/RunnableConfig arg the closure must accept (it does: config=None)? Does returning the node's dict update state correctly?
3. arun_incident (graph.py:73-85): builds a fresh graph per incident (build_graph called inside). Is rebuilding the graph every call a problem (performance/no state leak) — acceptable for this scale? The init dict builds Evidence(**{**e, "incident_id": ...}) — does the Evidence schema accept incident_id and all incident["evidence"] fields (id, ts, source, source_url, content)? Confirm against the actual Evidence model. Any KeyError if an evidence dict lacks source_url?
4. load_incident (graph.py:88-92): reads incidents/{incident_id}.json. Those files exist (INC-001.json ... INC-011.json). Confirm the JSON shape matches what arun_incident expects (id, window_start, window_end, description, evidence list). Any risk if incidents_dir is wrong or file missing (uncaught FileNotFoundError in CLI — acceptable?).
5. run_cli fake mode (graph.py:114-122): uses FakeLLMAdapter + make_collection + seed_prior — embeds a prior (INC-002) so recall returns consulted. Good for offline sample. The non-fake path (graph.py:123-127) uses LLMAdapter(live=live) + create_memory_collection(backend="auto"). In offline (live=False) replay, embeddings go through _default_embed -> LLMAdapter().embed -> record/replay or 256-dim fallback. Confirm dimension consistency: pre-seeded memory_seed (Phase 4) used _hash_embed (256-dim); the agent's embedded postmortems use the same collection embed_fn (_default_embed) so dims match. OK?
6. human_gate reject path through the graph: test_graph_rejects_when_verification_fails uses BadFakeLLMAdapter returning a postmortem with a hallucinated E99 claim. verify_postmortem sets verification_score=0.0, human_gate_node rejects (status "rejected"). But with score 0.0 and NO leak, memory_writer_node: human_decision=="rejected" -> returns {} (does not embed). So coll.count() stays 1 (only seeded INC-002), NOT 2. The test uses _assert_invariants(..., expect_approved=False) which asserts coll.count()==2 — IS THAT A BUG? Check: when rejected, is the postmortem still embedded? Look at memory_writer_node: it returns {} unless decision=="approved". So on rejection coll.count()==1. But _assert_invariants asserts coll.count()==2 unconditionally (line 71). Does the rejected test actually pass, or does it fail on the count assertion? Trace carefully. If it fails, that's a real test bug (count==2 only holds for approved).
7. Test coverage gaps: the replay test (test_graph_replay_e2e) is skipped offline (correct). Is there any test exercising the CLI entrypoint (graph.py main/run_cli) itself? Any test for load_incident? Any test that memory_writer refuses to embed a consult-only leak through the full graph (not just unit)? 
8. Determinism/reproducibility for Phase 8 eval: arun_incident rebuilds graph each call — fine. Any global state that would make repeated incidents (eval loop 4-10) share/leak state? The collection persists across incidents in the eval loop (memory grows) — intended. The sqlite conn is per-incident in tests (":memory:") but eval will use one conn — recall_incidents in memory_writer embeds only if approved. Confirm the graph never re-inits or drops the postmortem table mid-run.
9. Any real bug: e.g. _assert_invariants asserts pm.timeline/claims/action_items non-empty — does the FakeLLMAdapter's postmortem always populate those? The writer's citation retry: if the fake returns bad refs, does it loop or stop (exactly once)? Any unawaited coroutine in the graph path?
10. Severity call: BLOCKER (breaks spec/submission), BUG (wrong behavior), DESIGN-RISK (works but fragile/questionable), NIT. Focus on the count==2-in-rejection case (#6) — that is the most likely real defect.

Report format: concise bullets with severity, each with file:line and a one-line fix suggestion. End with "VERDICT: ship / fix-before-ship".