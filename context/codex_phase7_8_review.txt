You are reviewing Phase 7 (`api.py`, the CLI human-gate) and Phase 8 (`eval.py`, the judging-critical offline eval harness) of the "Agentic Incident Postmortem Synthesizer" (hackathon agentic-workflow entry). CODE REVIEW — do NOT modify files. Read them, critique, and report bugs, correctness issues, design risks, and test-coverage gaps with file:line evidence.

Files to review (project root):
- api.py  (Phase 7 CLI: `run`/`approve`/`reject`/`show` subcommands; owns a pending_approval gate distinct from the graph.py auto-approve gate)
- eval.py  (Phase 8: pre-seeds INC-001..003 into incident_memory, runs INC-004..011 through BOTH the full 7-node graph (`graph.arun_incident`) and a deterministic Baseline A stub, scores verification/red-herring/completeness, writes `evaluation_run` rows)
- tests/test_api.py  (Phase 7 lifecycle tests; assume green unless you find a real failure — I ran them: 14 passed, 2 skipped)
- tests/test_eval.py  (Phase 8 tests; assume green — I ran them, they pass)

Verified real API contracts these depend on (do NOT assume otherwise):
- agents.py nodes: `ingest_node(state,*,llm,tracer,conn,collection)`, `timeline_node`, `analyze_node` (returns `consulted`), `writer_node` (returns `postmortem`, citation guard retries once), `verifier_node` (calls `store.verify_postmortem(conn, incident_id, pm, recalled_incident_ids=...)`, NOW wrapped in try/except ValueError -> zero-score report, so it never raises on consult-only leak), `human_gate_node`, `memory_writer_node`. `analyze_node` fires `asyncio.create_task` recall via `ingest_node` (recall_task lives in state).
- graph.arun_incident(incident,*,llm,tracer,conn,collection) -> compiled StateGraph result dict with keys incident_id, postmortem, verification, candidates, consulted, human_decision, applied_incidents. Runs ALL 7 nodes incl. memory_writer (embeds non-leaking postmortems via memory.store_memory).
- store.py: `connect(":memory:")`, `init_db`, `insert_incident(conn, incident, status, ...)`, `insert_evidence(conn, incident_id, evidence_list)`, `verify_postmortem(conn, incident_id, pm, recalled_incident_ids=None)`, `insert_verification_rows(conn, incident_id, report)`, `upsert_postmortem(conn, incident_id, draft_json, verification_json=None, consulted_json=None, status="pending_approval", approved_by=None, time_approved=None)`, `insert_evaluation_run(conn, run_id, mode, model_name, incident_id, verification_score, red_herring_correct, run_time)`.
- memory.py: `is_consult_only_leak(postmortem, recalled_ids)`, `store_memory(postmortem, collection, verification_score=None)`, `recall_incidents`.
- rag.py: `create_memory_collection(name, embed_fn, backend="auto"|"memory"|"chroma", persist_dir=None)`, collection has async `add(id, document, metadata)`, async `recall(text, n, where)`, sync `count()`.
- schemas: Postmortem(incident_id, summary, impact, root_cause, timeline, action_items, claims, consulted_incidents), VerificationReport(incident_id, claim_reports, verification_score), Claim(statement, evidence_refs, from_recalled_incident=None), ConsultedIncident(incident_id, similarity_score, applied, note), RootCauseCandidate(rank, hypothesis, root_cause_label, supporting_evidence, contradicting_evidence, from_prior_incident).
- helpers.py: `FakeLLMAdapter` (scripted TimelineEvent/RootCauseCandidate/Postmortem keyed by schema; embed->hash vector), `make_collection()` (in-memory backend), `seed_prior(collection)` (seeds INC-002), `INCS` dict, `_good_postmortem()`.
- generate_incidents.py: `generate_incidents()` -> list of 11 incident dicts (INC-001..INC-011); `memory_doc(incident)` -> {incident_id, document, metadata}; `insert_incident`/`insert_evidence` used by eval baseline seed.
- tracer.py: `Tracer(incident_id, out_dir)` writes `{out_dir}/{incident_id}/{node}.json` + manifest.json via `.record(node, *, system_prompt, user_prompt, input_state, output, tool_calls=None, retries=None, human_decision=None, verifier_math=None)`.

Spec/judging context (hackathon):
- Reproducibility (15 pts): eval must run OFFLINE with NO key. `eval.py` default is `fake=True` (FakeLLMAdapter + hash embed). The `--live` path must SKIP cleanly when no key (or fixture) present. Determinism required: two offline runs -> identical scores.
- The eval's PRIMARY metric is the deterministic SQLite set-check verification_score (backed/total) — NOT semantic truth. Secondary: red-herring rejection (candidates[0].root_cause_label != incident["red_herring"]) and structural completeness.
- Baseline A must be an honest structural delta: same Postmortem schema, deliberately blames red_herring + cites hallucinated E99, so its verification_score = 0 and red_herring = 0 while completeness = 1. Agent must score >> baseline.
- §B.4 trajectories deliverable: every agent run writes trajectories/{incident_id}/{agent}.json + manifest.json. The CLI api.py writes its own (ingest/timeline/analyze/writer/verifier + human_gate/memory_writer on approve). The graph (used by eval) writes all 7.

Verify and report specifically:
1. api.py `run` pipeline (api.py:150-215 `_run_to_pending`): it manually calls ingest->timeline+analyze(gather)->writer->verifier but NOT human_gate/memory_writer, then upserts `pending_approval`. Does it correctly await `recall_task` (the analyze_node recall)? The graph uses state["recall_task"]; here `_run_to_pending` calls analyze_node directly via asyncio.create_task — does analyze_node internally await its recall, or does it rely on `state["recall_task"]` being awaited by the caller? Trace whether `consulted` is populated. Any missing await would mean `consulted` is empty on pending.
2. api.py `cmd_approve` (250-348): on approve it reopens a NEW collection via `_get_collection` (321). In tests they pass fake=True so it's in-memory; but in REAL persistent CLI use (db_path=./app.db), `_get_collection` uses `persist_dir=ROOT/chroma_db`. Does the `run` step's collection and the `approve` step's collection point at the SAME chroma store? If `run` used live (persistent chroma_db) and `approve` also uses persist_dir=chroma_db -> OK. But if `run` was `--fake` (in-memory) and `approve` is real (chroma_db), the approved postmortem embeds into a DIFFERENT store than the one recall used, so recall never sees it. Is that a real inconsistency? Also: `cmd_run` passes fake/live to `_get_collection` but `cmd_approve` reads `fake`/`live` only from its OWN flags (482-483) — so a user could `run --fake` then `approve` (no flag) and silently embed into chroma_db not the in-memory store. Flag as DESIGN-RISK.
3. eval.py `run_eval` (167-277): 
   (a) Pre-seed loop (205-210) adds `memory_doc(incs[sid])` to collection. But the agent path (`graph.arun_incident`) also calls `memory_writer_node` which embeds NON-leaking postmortems. So after running INC-004..011, the collection grows with newly approved postmortems (Option B: embeds all non-leaking). That means later eval incidents RECALL earlier eval incidents as "prior incidents" — contaminating recall with the same run's outputs. Is that intended (feedback loop) or a bug? The PRD says memory is consult-only / recall should be priors. Flag the cross-contamination risk: an eval incident's recall may surface a sibling eval incident, biasing red_herring.
   (b) `_verify_baseline` (128-146) inserts the incident + evidence fresh each time. If the agent path already inserted them, the `insert_incident`/`insert_evidence` swallow the exception (139-145). Does `insert_incident` use INSERT OR REPLACE (which would wipe evidence/postmortem from the agent run) or ON CONFLICT DO UPDATE? Check store.insert_incident — if it's INSERT OR REPLACE, the baseline seed would CASCADE-DELETE the agent's evidence rows, breaking the agent's verification rows. Trace this carefully.
   (c) The agent path uses `conn` (shared) and the baseline path also uses the SAME `conn`. The agent's `graph.arun_incident` calls `upsert_postmortem` and `verify_postmortem` (which inserts verification rows). Then baseline `_verify_baseline` re-inserts incident+evidence. If `insert_incident` is INSERT OR REPLACE, the agent's postmortem/verification for that incident could be clobbered. Confirm or refute.
4. eval.py determinism (310-313): `fake = True if not live else bool(args.fake)` — when `--live` is set, `fake` becomes `bool(args.fake)`. So `--live` alone sets fake=False (uses real LLMAdapter). But `--live` with no key -> `LLMAdapter(live=True)` will try the network and raise. The docstring says "The --live path is skipped offline (no key)". Where is it skipped? I don't see a skip — `main` calls `run_eval(live=live, fake=fake)` directly. If user runs `python eval.py --live` with no key, does it crash (CacheMissError / network) or gracefully skip? Check llm_adapter: live=True with no fixture -> _live_chat hits network. There's no skip. So `python eval.py --live` without a key CRASHES. Flag: the "skipped offline" claim is wrong; it would raise. (Only matters for live, not the default offline path.)
5. eval.py metrics: `red_herring_correct` for the AGENT uses `candidates` from `result.get("candidates", [])`. But the agent's `analyze_node` may recall a prior and the top candidate `root_cause_label` should NOT equal red_herring. For seeded INC-001..003 (which are priors), do any eval incidents (4-11) have red_herring that equals what FakeLLM returns as candidates[0].root_cause_label? The FakeLLM's analyze returns deterministic candidates — is candidates[0].root_cause_label guaranteed != red_herring for all eval incidents, so the agent gets red_herring=1.0? If FakeLLM returns the TRUE root cause label as candidates[0], and red_herring is planted distinctly, then agent red_herring=1.0 (good). But verify the FakeLLM candidate label matches incident truth, not red_herring. This is what makes the eval show agent >> baseline. If the FakeLLM's candidate label happens to equal red_herring for some incident, the agent's red_herring rate drops and the eval looks weak. Check helpers.FakeLLMAdapter's RootCauseCandidate emission.
6. Test coverage gaps: 
   - Is there a test that runs `eval.py --live` is skipped without key? (probably not — and per #4 it would crash, not skip).
   - Is there a test for the `run`->`approve` cross-store consistency (#2)? 
   - Is there a test that the agent's postmortem is NOT embedded before approve (pending gate respected)? 
   - Is there a test that `cmd_approve` refuses a consult-only leak (embeds=False)?
7. api.py `cmd_run` `_load_incident_arg` (71-95): if `arg` is an existing .json file it reads it directly and only validates `inc.get("id")`. But it does NOT validate the rest of the shape (window_start etc.) until cmd_run's own loop (231-233). However if the file is loaded directly, cmd_run still validates window_start etc. OK. But `_load_incident_arg` also accepts a bare `arg` that EXISTS as a file anywhere (e.g. `api.py run /etc/hostname`) if it ends in `.json` — wait, it checks `os.path.exists(arg) and arg.endswith(".json")`. A user could pass any .json file. That's by design (run <incident.json>). Fine. But note the path-traversal guard is only in `graph.load_incident`, not in `_load_incident_arg`'s direct-file branch. Minor.
8. Any real BUG: e.g. `cmd_approve` (299-308) re-upserts `verification_json=row["verification_json"]` (the ORIGINAL). But it applied `applied` flips to `pm` and `consulted`, then `upsert_postmortem(..., draft_json=pm.model_dump_json(), ...)` — good, persisted consulted with applied. But it does NOT update `ver` (VerificationReport) — fine. Does `status` transition correctly pending->approved? Yes. 
9. Severity call: BLOCKER (breaks spec/submission or eval gives wrong/misleading numbers), BUG (wrong behavior), DESIGN-RISK (works but fragile/questionable/likely-to-mislead-judges), NIT. Focus on: the eval cross-contamination (#3a), the INSERT OR REPLACE cascade risk (#3b/#3c — this could silently corrupt the agent's verification rows mid-eval), and the `eval.py --live` crash-without-skip (#4).

Report format: concise bullets with severity, each with file:line and a one-line fix suggestion. End with "VERDICT: ship / fix-before-ship".